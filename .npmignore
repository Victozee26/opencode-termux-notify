# although `files` in package.json is preferred, keep this as a safety net
# NOTE: do not list `dist/` here — `files` in package.json whitelists dist for publish
node_modules
.opencode
*.log
tmp
tsconfig.json
.git
.github
